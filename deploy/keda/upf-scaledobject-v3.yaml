apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: upf-scaler
  namespace: free5gc
spec:
  scaleTargetRef:
    name: free5gc-free5gc-upf-upf
  minReplicaCount: 0
  maxReplicaCount: 4
  pollingInterval: 10
  cooldownPeriod: 30
  triggers:
  # Trigger 1 : requêtes PDU entrantes (wake-up event-driven)
  - type: prometheus
    metadata:
      serverAddress: http://prometheus-kube-prometheus-prometheus.monitoring.svc.cluster.local:9090
      metricName: free5gc_pdu_session_requests_total
      query: increase(free5gc_pdu_session_requests_total[30s])
      threshold: "1"
  # Trigger 2 : throughput actif (scale-up supplémentaire)
  - type: prometheus
    metadata:
      serverAddress: http://prometheus-kube-prometheus-prometheus.monitoring.svc.cluster.local:9090
      metricName: free5gc_upf_throughput_bps
      query: sum(free5gc_upf_throughput_bps)
      threshold: "400000"
  # Trigger 3 : CPU > 80%
  - type: cpu
    metricType: Utilization
    metadata:
      value: "80"
